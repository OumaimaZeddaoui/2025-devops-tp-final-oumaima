# Backend Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/christmas_gifts?sslmode=disable
PORT=8080

# Frontend Configuration
# (Frontend uses Vite proxy configuration - no env vars needed)
